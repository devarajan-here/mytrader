# Create a new Gemini API key after revoking the key shared in chat.
GEMINI_API_KEY=replace_with_your_new_key

# Fast, current multimodal model. Override this if your project lacks access.
GEMINI_MODEL=gemini-3.7-flash

PORT=4173

# Optional local UI testing without making Gemini requests.
# MOCK_AI=true
